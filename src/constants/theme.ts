import { ThemeColors } from '../types/Track';

export const DEFAULT_ACCENT = '#1db954';
export const DEFAULT_BG = '#0e0e0e';
export const DEFAULT_SURFACE = '#1a1a1a';
export const DEFAULT_MUTED = '#888888';

export const DEFAULT_THEME: ThemeColors = {
  accent: DEFAULT_ACCENT,
  bg: DEFAULT_BG,
  surface: DEFAULT_SURFACE,
  muted: DEFAULT_MUTED,
  glow: 'rgba(29,185,84,0.18)',
};
